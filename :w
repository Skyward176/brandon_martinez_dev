'use client'
import * as React from 'react';
import {Navbar, NavbarBrand, NavbarContent, NavbarItem, Link, Button} from "@nextui-org/react";

const pages = ['About Me', 'Projects', 'Blog'];

function NavbarWrapper() {

  return (
    <Navbar>
      <NavbarBrand>
        <p className='text-4xl md:block'> Brandon Martinez </p>
      </NavbarBrand>
      <NavbarContent className='md-hidden'>
            {pages.map((page) => (
              <NavbarItem className='bg-black w-full text-center' key={page}>
                <p className="hover:bg-gray-500 font-extralight text-white text-xl">{page}</p>
              </NavbarItem>
            ))}
      </NavbarContent>
    </Navbar>
  );
}
export default NavbarWrapper;

